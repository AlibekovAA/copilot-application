services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: frontend_app
    restart: unless-stopped
    ports:
      - '3000:3000'
    environment:
      - NODE_ENV=production
      - NEXT_PUBLIC_API_URL=http://82.202.137.111:8000
      - NEXT_PUBLIC_AUTH_API_URL=http://82.202.137.111:8080
    networks:
      - app-network
    depends_on:
      - app
      - backend
    healthcheck:
      test:
        [
          'CMD',
          'wget',
          '--quiet',
          '--tries=1',
          '--spider',
          'http://82.202.137.111:3000',
        ]
      interval: 30s
      timeout: 10s
      retries: 3
