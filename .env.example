# Уровень логирования (числовые значения):
# 0 = DEBUG (самый подробный)
# 1 = INFO (информационные сообщения)
# 2 = WARNING (предупреждения)
# 3 = ERROR (ошибки)
# 4 = CRITICAL (критические ошибки)
LOG_LEVEL=0

# Директория для файлов логов
LOG_DIR=../logs

# База данных
DATABASE_URI=postgres://user:password@postgres_db:5432/dbname?sslmode=disable

# Для PostgreSQL-контейнера
POSTGRES_USER=user
POSTGRES_PASSWORD=password
POSTGRES_DB=dbname

# Для mistral
MISTRAL_API_KEY=you_token
# Модель Mistral AI для использования
# Доступные модели:
# - mistral-small-latest (рекомендуется, быстрая и качественная)
# - mistral-medium-latest (более мощная)
# - mistral-large-latest (самая мощная)
MISTRAL_MODEL=mistral-small-latest
MISTRAL_BASE_URL=https://api.mistral.ai/v1
# Таймаут запросов к Mistral AI в секундах
# По умолчанию: 60 секунд
MISTRAL_TIMEOUT=60.0

#Токен для передачи данных для авторизации
JWT_SECRET=jwt_token
